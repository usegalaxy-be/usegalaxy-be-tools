name: Update Trusted Tools

on: push
  # schedule:
  #   # * is a special character in YAML so you have to quote this string
  #   # We'll run this weekly at 9.
  #   - cron:  '12 9 * * 1'
  # workflow_dispatch:

jobs:
  call-workflow:
    uses: ./.github/workflows/update-tool-list.yml
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.7]
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      # - name: Fetch tools from usegalaxy-eu-tools
      #   uses: ./.github/workflows/update-tool-list.yml
      - name: Perform linting
        run: |
            # Update the trusted tools.
            make fix -j $(nproc)
            # make lint -j $(nproc)
            make update_trusted -j $(nproc)
      - name: Show differences
        run: "git diff --stat"
      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: Update Galaxy tools
          title: "Automatic updating of the tools in tools-iuc.yaml"
          body: |
            This PR is automatically generated based on changes in the [usegalaxy.* tools](https://github.com/usegalaxy-eu/usegalaxy-eu-tools) tools
            and updates to our own tools and new tools added.
            Please verify if these changes are valid.
          labels: |
            automatic PR
          reviewers: ieguinoa
          branch: tools-update
          base: master

      - name: Check output
        run: |
          echo "Pull Request Number - ${{ steps.cpr.outputs.pull-request-number }}"

      # - name: Commit and files
      #   run: |
      #     git config --local user.email "github-bot@github.com"
      #     git config --local user.name "github-actions[bot]"
      #     git add *.lock
      #     git commit -m "Updated trusted tools ($(date -I))" || true
      #     git push

      # - name: Push changes
      #   uses: ad-m/github-push-action@git-actions-test
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     branch: ${{ github.ref }}
